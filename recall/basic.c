#include <stdio.h> //importing a library for standard input/output

int main () {//entry point of the program. Basically saying, start running my program from here. int = the type of value it returns (an integer)
    printf("Hello, I am back to C\n");
    return 0;//return a value - excecuted succesfully. The program stops when it gets here. It sends a signal to the operating system.
}